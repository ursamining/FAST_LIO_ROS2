/**:
  ros__parameters:
    feature_extract_enable: false
    point_filter_num: 1
    max_iteration: 5
    filter_size_surf: 0.3
    filter_size_map: 0.3
    cube_side_length: 1000.0
    runtime_pos_log_enable: false
    map_file_path: "./test.pcd"

    common:
      lid_topic: "/lidar_points"
      imu_topic: "/lidar_imu"
      time_sync_en: false # Keep false since Unitree L2 has hardware-synchronized IMU and LiDAR
      time_offset_lidar_to_imu:
        0.0 # Time offset between lidar and IMU calibrated by other algorithms, e.g. LI-Init (can be found in README).
        # This param will take effect no matter what time_sync_en is. So if the time offset is not known exactly, please set as 0.0

    preprocess:
      lidar_type: 0 # 0 for default pipeline, 1 for Livox serials LiDAR, 2 for Velodyne LiDAR, 3 for ouster LiDAR, 5 for Unitree L2
      scan_line: 128 # We use the 2 for now but we should implement 6 that uses the firetimes for better precision
      timestamp_unit: 3 # 0-second, 1-milisecond, 2-microsecond, 3-nanosecond.
      blind: 0.5 # minimum detection range in meters

    mapping:
      acc_cov: 0.5
      gyr_cov: 0.5
      b_acc_cov: 0.001
      b_gyr_cov: 0.001
      fov_degree: 360.0
      det_range: 60.0 # JT128 L2 detection range up to 30m
      extrinsic_est_en: false # true: enable the online estimation of IMU-LiDAR extrinsic
      extrinsic_T: [-0.0076, 0.01367, 0.01274] # IMU offset from LiDAR center (from hesai docs, otherwise use 0)
      extrinsic_R: [1., 0., 0., 0., 1., 0., 0., 0., 1.]

    publish:
      path_en: false
      scan_publish_en: true # false: close all the point cloud output
      dense_publish_en: true # false: low down the points number in a global-frame point clouds scan.
      scan_bodyframe_pub_en: true # true: output the point cloud scans in IMU-body-frame

    pcd_save:
      pcd_save_en: true
      interval:
        -1 # how many LiDAR frames saved in each pcd file;
        # -1 : all frames will be saved in ONE pcd file, may lead to memory crash when having too much frames.
